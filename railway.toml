[build]
builder = "NIXPACKS"

[deploy]
startCommand = "apt-get update && playwright install-deps chromium && playwright install chromium && python scraper.py"
cronSchedule = "*/5 * * * *"
restartPolicyType = "never"
